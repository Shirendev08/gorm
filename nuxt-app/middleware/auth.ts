// export default function ({ redirect, $cookies }) {
//     // Check if the user is logged in (e.g., a token exists in cookies)
//     const token = $cookies.get('authToken'); // Replace 'authToken' with your actual token key
  
//     // If the user is logged in, redirect them to the homepage or another protected page
//     if (token) {
//       return redirect('/');
//     }
//   }
  